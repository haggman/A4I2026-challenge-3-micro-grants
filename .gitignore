# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# Notebooks
.ipynb_checkpoints/

# Credentials - never commit these
*.json.key
service-account*.json
credentials.json
.env

# OS noise
.DS_Store
Thumbs.db

# Local data pulls. The two BEA files are DELIBERATE exceptions - they are the
# pinned coefficient table the notebook depends on, and the concordance used to
# build it, so the mapping can be audited rather than taken on trust.
data/*.parquet
data/*.csv.gz
data/*.csv
!data/bea_direct_requirements.csv
!data/bea_naics_concordance.csv
